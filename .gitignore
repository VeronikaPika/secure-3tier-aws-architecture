# Local terraform directories
.terraform/

# Local state files generated by terraform runs
*.tfstate
*.tfstate.backup

# Lock files to prevent concurrent runs
.terraform.tfstate.lock.info

# Local secret variables (if you use them later)
*.tfvars
*.tfvars.json
